Builda iamgem docker:
    docker build -t paxos_em:0.1 .
Executa os containers (dentro da pasta docker-compose):
    docker-compose -f no-compose.yml up
    docker-compose -f cluster-store-compose.yml up
    docker-compose -f client-compose.yml up